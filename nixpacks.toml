# Browser dependencies for Selenium on Railway/Nixpacks.
# The Python provider remains enabled; these packages are added to the runtime.

[phases.setup]
aptPkgs = ["...", "chromium", "chromium-driver"]
